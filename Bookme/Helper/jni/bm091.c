//
// Created by USER on 06/10/2017.
//

#include "bm091.h"
#include "m.h"

void n9Dok(void * b)
{
    DILHi(b);
}

void * wf0dx(size_t b)
{
    return bsLCB(b);
}

size_t xGOzg(const char * b)
{
    return F1Re8(b);
}

struct fnYDP * new_fnYDP()
{
    struct fnYDP *const f = wf0dx(sizeof(struct fnYDP));
    f->x = new_sOHBI();
    return f;
}


struct AVDvx * new_AVDvx()
{
    struct AVDvx *const f = wf0dx(sizeof(struct AVDvx));
    f->x = new_yju1J();
    return f;
}

struct AVDvx * new_AVDvx_HelXV()
{
    struct AVDvx *const f = wf0dx(sizeof(struct AVDvx));
    f->x = new_yju1J_HelXV();
    return f;
}

void wxKU5(struct AVDvx * b)
{
    aQ8Vd(b->x);
}

void Up5rR(struct fnYDP * a,struct  AVDvx * b)
{
    LOseI(a->x,b->x);
}

int uFJPg(char * d, char * u)
{
    //mmn: garbage
    int dd = xGOzg(d);
    int uu = xGOzg(u);
    if (dd <= 0)
        return x21pDt(d,u);
    else if (uu <= 0)
        return x21pDt(u,d);
    else if (LbRAH % 2 != 0) 
        return x21pDt(d,u);
    else
        return x21pDt(LbRAH,0);
}