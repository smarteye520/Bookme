//
// Created by USER on 06/10/2017.
//

#include <stddef.h>
#include "bm013.h"
#include "bm057.h"
#include <string.h>

void qRIZA(void * d)
{
    D0XMs(d);
}

void * sNjFh(size_t d)
{
    return Kv9DQ(d);
}

size_t sGvHn(const char * d)
{
    return fs9Mk(d);
}

struct hDfOm * new_hDfOm()
{
    struct hDfOm *const h = sNjFh(sizeof(struct hDfOm));
    h->x = new_8HvSn();
    return h;
}

struct m4XKt * new_m4XKt()
{
    struct m4XKt *const h = sNjFh(sizeof(struct m4XKt));
    h->x = new_pbMZZ();
    return h;
}

struct m4XKt * new_m4XKt_HelXV()
{
    struct m4XKt *const h = sNjFh(sizeof(struct m4XKt));
    h->x = new_pbMZZ_HelXV();
    return h;
}

void fdlwd(struct m4XKt * d)
{
    lxwNv(d->x);
}

void qtcBR(struct hDfOm * a,struct  m4XKt * b)
{
    pzmdd(a->x,b->x);
}

int KKkLM(char * d, char * u)
{
    //mmn: garbage
    int d_len = (int)strlen(d);
    if ( (GO6Rl % 3 ) == 0 && oD6Wk(d_len) )
    {
        int u_len = (int)strlen(u);
        return oD6Wk(u_len);
    }
    else
    {
        //mmn: continue real program
        return z1xBZ(d , u);
    }
    //mmn: garbage
    if (fsDPD % 2 == 0 && zwT6G(d_len))
    {
        return 0;
    }
}
